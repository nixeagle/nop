#pragma once

// WARNING! This is automatically included in all other files.
// We do this for simplicity and to avoid repeating some common includes
// over and over.

// Up first is our type definitions. Everything in the project is based on
// having access to these 'simple' types.
#include "types.h"
