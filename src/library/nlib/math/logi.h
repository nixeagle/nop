namespace nib {
  namespace math {
    /// logirithm in base 10 returning an integer.
    /// This gets the length of a number in base 10.
    unsigned int logi(unsigned int number, unsigned int base);
  }
}
